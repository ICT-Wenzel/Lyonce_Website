<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    <link rel="stylesheet" href="{{ 'styles.css' | asset_url }}">
</head>
<body>
    {% section 'header' %}
    
    <main>
        {{ content_for_layout }}
    </main>
    
    <footer>
        <p>&copy; {{ 'now' | date: '%Y' }} Your Store Name. All rights reserved.</p>
    </footer>
    
    <script src="{{ 'scripts.js' | asset_url }}"></script>
</body>
</html>